# Easypanel / Nixpacks — static Vite admin (not `npm run dev` in production)
[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
